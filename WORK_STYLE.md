# proto-10 作業スタイルガイド

このファイルはコンテンツ作製・ファイル操作に関するユーザーの作業スタイル方針を記載する。
コンテンツ作製・大量書き込み作業の前に必ず参照すること。

---

## JSONファイルの作成・書き込み方針

### 基本ルール: 先にファイルを作成してから、1件ずつ書き込む

大量のJSONアイテム（クイズ・ガイドなど）を新規作成する場合は、以下の手順を踏むこと：

1. **空のJSONファイルを先に作成する**（ヘッダー・配列の骨格のみ）
2. **1件ずつ内容を書き込む**（Edit ツールで追記）
3. 各件の書き込み後に進捗を報告する

### 理由

- 一括書き込みだと途中でエラーが起きたときにどこまで書けたか分からない
- 1件ずつ書き込むことで進捗が可視化され、ユーザーが確認・中断しやすい

### 悪い例（やってはいけない）

```
// 60件全部を一度にWriteツールで書き込む → NG
```

### 良い例

```
// 1. 空ファイルを先に作成
// 2. quiz2_odaiba_001 を書き込み → 報告
// 3. quiz2_odaiba_002 を書き込み → 報告
// ...
```

---

## その他の作業方針

- **バックアップ**: 既存ファイルを修正する前に必ず `.bak_YYYYMMDD` バックアップを作成する
- **GitHubへのpush**: 必ず確認を取ってから実行する（`git push origin master`）
- **CONTENT_FEEDBACK.md**: コンテンツ作製・修正時は必ず参照し、新しい指摘も追記する
- **確認なしで作業を進める**: push以外は基本的に確認なしで進める

---

## 参照すべきファイル一覧

| ファイル | 内容 | タイミング |
|---|---|---|
| `CONTENT_FEEDBACK.md` | コンテンツ品質・内容に関するフィードバック | コンテンツ作製・修正時 |
| `WORK_STYLE.md`（このファイル） | 作業手順・ファイル操作の方針 | 大量データ作業・ファイル作成時 |
